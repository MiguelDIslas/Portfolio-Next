
import Spinner from '@components/general/Spinner';

/**
 * This component is used to render the loading page.
 * @returns {JSX.Element} Loading page component.
 */
const Loading = (): JSX.Element => <Spinner />;

export default Loading;
